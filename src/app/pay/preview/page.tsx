import { PaymentWorkspace } from '@/features/payments/payment-workspace';
export const metadata={title:'Username flow · example preview',robots:{index:false,follow:false}};
export default function Preview(){return <PaymentWorkspace preview/>;}
