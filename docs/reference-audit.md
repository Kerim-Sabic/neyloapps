# Reference audit — 12 September 2026

Inspected https://cloudflare.pay/ in the Codex browser at 1440×1000 and 390×844 before designing. Evidence is in the handover's `evidence/reference-1440.png`, `reference-390.png`, and `reference-typed-1440.png`. The browser's full-page raster capture includes surplus canvas; layout measurements below come from rendered DOM bounding boxes, not estimates from the raster.

Desktop: a minimal navigation bar, centered 720px-wide landscape card above the heading, generous whitespace, a single narrow input, centered action, and restrained footer. The card measured 720×409.08px with 24px radius. The header measured 1060px wide and 82px high. The headline occupied a 680px-wide centered box and was two lines. There are no feature grids, phone images, or split-column marketing panels.

Mobile: at 390px viewport the card measured 360×204.54px, positioned 15px from either edge, top 118px. The centered heading wraps to three lines; a single-column form follows. Small footer content sits below the primary flow.

Observed interaction: entering `neylo_test` immediately changed the card handle and action label. No reservation was submitted. The card showed a warm material texture/light pattern in the screenshots. Exact tilt amplitude and full motion timing were not measured; do not claim reference motion equivalence.

NEYLO translation: retain the card-first centered composition, approximately 1.76:1 reference geometry, narrow form and compact navigation. Reduce vertical gaps to accommodate two required fields and prominent offer qualifications. Use original sapphire material, a custom N line motif, NEYLO type/copy, and state-derived credit information. Proposed motion: one 600ms reveal, restrained fine-pointer tilt, stable typing, and an explicit still QR sheet. Replace every reference trademark, claim, account/payment label, link, code and asset.
