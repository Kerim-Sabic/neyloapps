# Verification record — 12 September 2026

## Passed

- Strict TypeScript and five focused unit checks.
- 23 checks against isolated real Supabase Postgres. Concurrent claim ownership, a 115-account founding-capacity race capped at 100, idempotent completion and referral retries, six competing referrals capped at three rewards/250 KM, immutable reversal retries, expiry, self-referrals, wrong identities, unverified identities, permissions/RLS, staff exclusions, pause/review/deletion, durable rate limiting, and ledger reconciliation.
- Two independent local browser cookie sessions used real provider OTPs captured by Mailpit. A completed at 100 KM; B completed via A's invitation at 100 KM; A became 150 KM with exactly one rewarded friend. Both survived refresh, sign-out and fresh provider sign-in. The actual validation/Judge views showed two completed accounts, one referral and 250 KM total. The shared CSV implementation reconciled with these database records.
- Production Horalix and Gmail inboxes received real messages through Resend and supplied codes that Supabase verified. The owner completed `@kerim`, received operator access, and opened the private production dashboard. The separate Gmail HTTP cookie session completed `@qa_delivery`, persisted through repeated account reads, safely retried finalization and received 403 from the admin endpoint.
- Production invitation attribution persisted for the excluded test account and correctly awarded nothing.
- The user independently exercised two further production signups, `@kerimsabic` and `@noomy`. Before classification, the real ledger held 150 KM and 100 KM respectively, including exactly one 50 KM referral. The user then confirmed they were tests. They were classified `test` and all three grants were reversed through audited immutable RPCs. No fake OTP or manual email confirmation was used. Their historical two founding allocations remain consumed, leaving 98 available; reversals do not reopen slots.
- Production HTTPS, HTTP/www canonical redirects with query preservation, public routes, deep links, same-origin mutation rejection, unauthorized account/admin/CSV/readiness denial, private/no-store policies, and the referenced CSS/JavaScript files were checked. A release-only static-asset routing regression was detected in the browser, corrected, and added to the release gate before recruitment continued.
- All five managed migrations are applied. Managed anonymous privileged-RPC/private-table attempts were denied. The live ledger's five reconciliation assertions pass. The client artifact scan found no service-role key, rate-limit secret or local backend URL.
- The real QR raster decoded to its exact canonical invitation URL. The sheet is still and high contrast; keyboard Escape closes it and restores focus to the trigger.
- Live browser viewport checks at 320, 360, 390, 430, 768 and 1440 px found no horizontal document overflow. The host browser uses a 15 px desktop scrollbar. A separate read-only iframe inspection without desktop scrollbars confirmed the intended 390 px mobile card at 360 × 204.54 px, x=15/y=118, and the 1440 px desktop card at 720 × 409.08 px. Input text measured 16 px.
- The user's follow-up code-entry redesign provides one semantic native input with six evenly spaced presentation slots, an active position, numeric keyboard/autofill hints and full-code paste. Browser checks covered a leading zero, pasting `012 345` as six digits, incorrect-code feedback, then a real local provider code restoring the same account/balance. It was deployed without pausing enrollment.

## Evidence and limitations

The handover contains machine-readable database, production release, reconciliation, referral, email and viewport reports; local and production CSV snapshots; actual browser screenshots; and a short sequence of captured UI states. Local test data never counted as real recruitment. The four production test/staff accounts are excluded from participant/Judge totals. Live counts may change as real recruitment begins; the report timestamps define each snapshot.

The in-app browser initially ignored viewport overrides and its full-page raster capture sometimes added surplus canvas or duplicated a lower page segment. Viewport-only captures and DOM measurements are the reliable final visual evidence. The frame sequence is not a high-frame-rate recording: it cannot establish entrance/flip timing or rule out every motion jitter artifact.

Physical phones, an actual mobile virtual keyboard, OS-level reduced-motion emulation, 200% browser zoom and a physical camera QR scan were not available through the enabled browser tools. Reduced-motion branches and numeric/autofill semantics were inspected in source; no claim of an actual OS/screen-reader test is made. QR decoding is a software scan of an actual browser raster. No Lighthouse/mobile-network score, penetration-test certification, KYC/unique-human proof, backup restore drill or external uptime monitor is claimed.

The active release uses Workers Free and a beta vinext adapter. The email throttle currently allows 30 messages/hour at Supabase; limits and failures are surfaced honestly. No paid service, upgrade or domain transfer was performed. Production signup remains open during the final code-entry refinement.
